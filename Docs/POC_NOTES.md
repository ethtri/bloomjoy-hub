# POC Notes (Loveable Generated) — Inventory & Findings

Purpose: capture what the existing Loveable POC already has so agents can extend it without breaking it.

## What we know
- Stack: Vite + React + TypeScript + Tailwind + shadcn/ui
- Source: Loveable-generated POC (do not rewrite from scratch)

## To fill in during intake (P0)
### Routing
- Router library:
- Where routes live:
- Public routes implemented:
- Any portal/auth-gated routes present:

### State/data fetching
- Supabase present? (Y/N)
- Stripe present? (Y/N)
- Data fetching library (if any):

### Folder structure snapshot
- Key folders:
- Where UI components live:

### Environment variables
- `.env.example` exists? (Y/N)
- Client env var prefixing (`VITE_`) handled? (Y/N)
- Any accidental secrets in client config? (Y/N)

### Known issues / tech debt
- Build warnings:
- Console errors:
- Dead code / unused deps:
