# Current Status

Last compacted: 2026-08-12

GitHub Issues and the Bloomjoy Project board are the operational source of truth for active work, priority, blockers, acceptance criteria, and closeout evidence.

## Live Work

- Run `npm run agent:github-hygiene` before planning sprint work, branch syncs, or broad closeout.
- Use issue labels, project status, PR checks, and latest issue/PR comments instead of static docs for task state.
- Keep active blockers, UAT evidence, and closeout notes on the issue or PR where the work is happening.
- Do not add running task chronology, stale PR lists, old branch names, or historical completed-work ledgers here.

## Durable References

- Product and decisions: `Docs/DECISIONS.md`; use `PRODUCT.md` or `DESIGN.md` only when they exist in the repo
- Local setup and verification: `Docs/LOCAL_DEV.md`, `Docs/QA_SMOKE_TEST_CHECKLIST.md`
- Production operations: `Docs/PRODUCTION_RUNBOOK.md`
- Architecture and scope: `Docs/ARCHITECTURE.md`, `Docs/MVP_SCOPE.md`

## Current Themes

- **Legacy refund-state truthfulness (`#784`, `#793`):** the original database-owner-only repair is deployed but has not touched the live row. Aggregate production evidence showed the frozen case has exactly one sent confirmation plus one sent approval, so a new forward-only repair accepts only that exact two-message history and rejects the former one-approval-only shape, every other count/type/status, any provider attempt, and any completion/adjustment evidence. It preserves both sent messages unchanged, returns current state to review, clears stale current match/recommendation fields, records only redacted aggregate evidence, and keeps decisions, customer messages, and provider attempts frozen until a fresh read-only transaction check. The change is not automatic, is not yet deployed, and cannot call Nayax or Gmail.
- **Refund production-readiness release:** the current production-safe foundation includes the email-only pilot, exact mapped-manager/fresh-TOTP authority, deterministic follow-ups, duplicate reconciliation, manager aging, retention controls, provider-outcome orchestration, and the attachment-free evidence-only manager workflow. Choosing a transaction cannot approve, pay, close, or email. Official payment authority comes only from the current exact-machine manager mapping; separate Admin access neither grants that authority nor invalidates a real mapping. The strict two-step Nayax adapter and atomic caps remain unreachable from the production handler. Production Gmail schedules, automatic customer contact, official actions, GPT, manager aging, and live Nayax execution remain off.
- **Launch boundary:** exact OAuth for the directly connected production customer-service mailbox, `info@bloomjoysweets.com`, with Gmail read-only/send scopes; the verified Info/Support/Refunds send-as mailbox identities; the isolated label/sender population; legacy-responder exclusion for that population; one original-thread `refund_first_contact_v1` acknowledgement; replay/later-reply suppression; and teardown back to disabled all passed with synthetic evidence on 2026-08-12 PT. A verified send-as address counts as Bloomjoy mailbox-origin only with provider `SENT` evidence and the existing delivery gates. After the native date/time fix in `#773`, the same private email-context path completed one existing Gmail draft through the hosted form without a duplicate case; `#774` made direct and email-linked intake use the same manager-assignment rule. Case-specific original-thread delivery from the designated support mailbox with the complete current mapped-manager CC set is still unproved in the bounded mailbox window, so the legacy responder remains authoritative for normal mail and broad Gmail polling/customer contact remain off. The approved 180-day retention policy applies to the Hub's sanitized Gmail copy, not the source mailbox or the canonical refund case; the first retention-only run passed with Gmail access skipped and zero eligible copies/attachments, while its recurring schedule remains off. EasyText/SMS remains outside this pilot.
- Payment-first storefront policy is tracked in P0 `#715`: the Commercial Machine is the only quote/request product; available sugar, Bloomjoy branded sticks, Micro Machine, and Plus checkout before Ethan/Ian and WeCom alerts. Mini and custom sticks remain visibly unavailable until their complete payment-first pricing and fulfillment rules are ready. Micro keeps its `$2,200` planning price but checkout is client- and server-gated off by default until the open executive shipping decision in `#717` is resolved; it never falls back to an unpaid request. California Stripe Tax launch approval is tracked separately in P0 `#718`. The CDTFA seller's permit is current from 2026-01-01 with a yearly period ending 2026-12-31; Stripe's live and sandbox head-office records match the privately confirmed Mountain View location, live Sugar products use `txcd_40020004`, and filing/remittance ownership is documented in `Docs/SALES_TAX_OPERATIONS.md`. Live California collection was owner-approved and activated on 2026-08-08 at 16:58 UTC. The reviewed Sugar, branded-sticks, and Plus checkout creators were deployed at 17:18:21 UTC; live no-payment evidence confirms Automatic Tax for Sugar and sticks, California Sugar exemption, positive California sticks tax, and no collection for a no-registration destination. The live Stripe Plus product description matches the approved flat `$100/month` per-customer-account model without changing its price, Price reference, or tax configuration. PR `#716` is merged and its reviewed frontend was promoted to the production domains after clearing a stale Vercel Instant Rollback. It adds the portal-side authenticated checkout action, resolves every known Stripe Customer and Plus subscription before acting, treats Stripe as authoritative over stale local subscription state, rejects conflicting Customer or subscription ownership metadata, fails closed on ambiguous histories, resumes an existing incomplete Checkout instead of sending it to Billing, and uses a durable per-account creation lease plus Stripe idempotency to prevent concurrent or timeout-retry duplicate Checkout Sessions. Post-deployment no-payment smoke identified one additional recovery boundary: explicitly expiring a diagnostic Session before its original Stripe `expires_at` left the durable `ready` row reusable. The forward-only `202608100001_plus_expired_checkout_recovery.sql` migration and matching `stripe-plus-checkout` recovery path make the provider's no-open-session result authoritative, release only the signed-in account's matching stale row, and preserve one-session concurrency protection. Failed-payment recovery, scheduled-cancellation renewal, terminal-subscription restart, duplicate fallback, incomplete-checkout recovery, early-expiry recovery, and billing-refresh failure are covered by database, unit, persona, and authenticated no-payment UAT. The billing portal uses an explicit reviewed Stripe configuration with payment-method updates, invoice history, and end-of-period cancellation/renewal. Shipping treatment and the best Bloomjoy Plus product-code mapping remain documented working positions. Release acceptance requires zero open diagnostic Checkout Sessions, no payment/order/subscription/entitlement/communication side effects, clean Stripe ownership, the reviewed tax/webhook behavior, and both client/server Micro gates remaining off.
- The deployed safe release is aligned to ten reviewed Refund Operations functions and 47 refund/Nayax migrations. Deterministic Nayax recommendation, the streamlined manager workbench, automation health, Gmail thread linkage, owner TOTP readiness, legacy-state normalization, and the human-review-only GPT runner are present behind independent default-off controls. The legacy Google Form/Sheet/AppSheet workflow remains the fallback.
- Machine-QR refund intake (`#662`) keeps customer-reported time separate from verified QR-open time. It can recommend either a strong card match or exactly one machine/amount/QR/time match; no recommendation proves a delivery failure or approves a refund. Any official use still depends on the exact mapped manager, fresh TOTP, global official-action gate, Nayax contract, reconciliation path, caps, and supervised pilot.
- P0 `#750` is merged, owner-approved, and deployed to production. The hosted form records customer-described payment interaction, wallet provider, time accuracy, and issue category without treating those statements as Nayax facts. Follow-up `#753` removes the redundant customer product/selection question because the selected machine determines whether the purchase is a phone case or cotton candy; legacy stored product notes remain readable. Matching policy `2026-08-11.v3` keeps rough or hour-wide customer times in manager review; optional Nayax product, configured-price, current-status, and nearby-alert context is read-only, fail-soft, and does not add ranking points. The manager workbench presents one customer-versus-machine comparison, plain-language reasons, collapsed alternatives/context, and one primary manager action. Routine system health stays out of the task; only actionable delivery/reminder warnings appear. Route, portfolio-option, and Nayax-mapping smokes pass with live Nayax execution and all optional assistant schedules still disabled.
- The approved refund target is one manager decision, not a manual Nayax workflow (`#674`). Bloomjoy should automatically attempt matching, use the secure customer wallet-detail correction loop in `#673` when useful, and show the manager one **Approve refund** or **Decline** decision only after one safe transaction is identified. Confirmed provider success should create one original-thread customer completion message with the complete current mapped-manager set visibly CC'd; it must not send a separate duplicate manager completion email.
- The `#673` wallet-correction slice is merged behind default-off customer-contact controls. A wallet no-match may receive one 48-hour single-use correction link plus at most one replacement reminder; the form accepts only wallet type, virtual/device last four, approximate local purchase time, and amount confirmation. Submission preserves the machine and QR evidence, automatically re-runs deterministic Nayax matching, notifies managers only for a high-confidence transaction, and otherwise marks the case eligible for the still-TBD fallback route. Raw link tokens and forbidden payment credentials are not stored.
- The current merged release passed the full repository build/lint/test profile, 141 disposable-project migrations, 27 pgTAP files with 1,069 assertions, the 42-state hosted Refund UAT, and release validation for ten functions and 45 migrations. Production route, portfolio-option, manager-assignment, and Nayax-mapping smokes pass. This is verified safe-foundation evidence, not a live payment or broad inbox launch.
- Production currently exposes all 23 active eligible Commercial/Mini machines as non-duplicate customer-safe refund options. Manager/Nayax automation readiness remains a separate setup state: seven machines currently pass the shadow-readiness audit with one to three active managers, one Nayax mapping, and a location timezone, while live refund execution remains disabled. Internal `Unmapped`/`Unknown` labels remain excluded. Owner-approved mapped-manager TOTP setup, controlled provider pilots, and the named shadow pilot remain pending; a separate clean-manager persona remains useful for assigned-only access testing but is not required to make a legitimate dual-role mapping valid.
- Live Nayax execution remains disabled and sponsor-gated. Nayax's public documentation confirms a two-step refund request/approval flow and documents `SiteID` in Last Sales, but Bloomjoy's captured production response omitted that field and the existing token was obtained for reporting. The vendor escalation is acknowledged but has not supplied the substantive account-specific write contract. Before the one-approval target can execute a live refund, `#430` must confirm production endpoint/token write authority, amount and response semantics, idempotency/reconciliation behavior, a production `SiteID` source, and explicit approved-sale evidence (or approved substitutes); `#767` must supply an audited unknown/rejected-outcome resolution path; then the owner must approve caps, allowlist, and one supervised low-value test.
- Alternative compensation is now a P0 dependency, but its provider remains undecided (`#666`). It is offered only after bounded matching/customer-correction attempts reach a terminal unmatched state, or for ineligible payment methods such as cash. An unknown provider outcome must be reconciled before compensation so one claim cannot receive both remedies.
- `info@bloomjoysweets.com` is the directly connected production customer-service mailbox. Its verified Info/Support/Refunds send-as identities remain identities of that mailbox, subject to provider `SENT` evidence and every existing delivery gate. `etrifari@bloomjoysweets.com` and its plus-addresses may be used only as an owner-controlled synthetic customer/test sender or recipient, or for vendor/account correspondence; they are not the production refund-assistant mailbox. The owner approved attachment-free operation, the 180-day sanitized-copy policy, and visible complete current mapped-manager CC for case-specific messages. The isolated first-contact/original-thread proof and private-form continuation passed; after machine resolution, the remaining email gate is a bounded case-specific reply originating through the designated support mailbox in the original Gmail thread with the complete current mapped-manager CC route, followed by an explicit active-cutover decision. Hosted-form intake and manual case handling remain independent.
- Refund intake ownership now uses one database rule for direct website submissions and private email-linked form completion: exactly one current active mapping is assigned under the Admin Machines lock; zero or multiple mappings remain unassigned for explicit review. The release also repairs eligible existing open unassigned cases with a redacted audit event, while all Gmail, automatic contact, official action, and Nayax switches stay off.
- The exact current machine mapping is the sole official-action grant. Admin access alone cannot act, while a mapped manager who also holds Super Admin or Scoped Admin access remains valid only through that mapping. Production verification passed for the selected mapped dual-role owner/case, but the global official-action gate remains false.
- Automation scheduler credentials and endpoint plumbing are configured without enabling the lane. A PII-free forced-failure alert, exact-key replay suppression, and a disabled-lane no-op passed in production-safe runs. `#632` still needs one staffed synthetic due reminder/escalation, manager-visible healthy state, replay, and teardown; GitHub/Edge automation and manager-aging switches remain false.
- Fresh action-bound TOTP and receipt protections are implemented, synthetically verified, and deployed default-closed. P0 `#782` is merged through `#785`/`#787` with a private immutable Auth-UUID digest (not an email hash), lock-time owner/admin/confirmed-identity/mapping revalidation, one self-only five-minute database window, and expiry/cancel/success cleanup with sanitized audit. Hosted Auth is restored to enrollment-off/verification-on, and P0 `#789`'s exact-project, GET-only live closed-state gate passed immediately before and after the two refund migrations and two manager functions were deployed. No enrollment window was opened, no QR/code was exposed, and official payment, Nayax, Gmail, customer-contact, GPT, automation, and manager-aging gates stayed off.
- Scheduled production drift remains a release blocker in `#768`: local read-only comparison passes ten functions and 47 migrations, but GitHub must not receive a broad owner PAT. The monitor stays fail-closed until a Bloomjoy-project-only Edge Functions Read credential is privately created, validated, stored in the protected environment, and proven on `main`.
- GPT triage remains default-off and human-review-only. The server runner, strict schema, content-free job ledger, and local developer credential destination are implemented; no production OpenAI secret is configured. A new fail-closed server acknowledgement prevents provider execution until `#635` records the exact OpenAI project retention/data-control mode and privacy/security approval; `store=false` is not treated as zero-retention proof. The database structurally prevents automatic sending.
- Refund operations and customer-refund pilot readiness remain high-sensitivity operational surfaces.
- Access grants that create user-facing Corporate Partner or Technician access must prove both the saved grant and the invite-email attempt; rollout signoff also needs provider/inbox/activation evidence in the linked issue or PR.
- Partner activation uses manual Email Codes and a temporary non-persisting auth session; password creation must succeed before portal sign-in. Hosted signup-confirmation, invite, Email Code, and recovery templates must pass `npm run auth:templates:validate` so email-security prefetch cannot consume credentials. Production Auth delivery uses Bloomjoy's Resend custom SMTP; Supabase's two-email/hour demonstration sender is not an acceptable production fallback.
- Technician management IA uses `/portal/team` for Plus Customer and Corporate Partner self-service, while `/admin/access` remains the Super Admin override surface and the Scoped Admin machine-scoped Technician grant surface. Plus/partner users should not receive `/admin` just to manage Technicians.
- Admin Console IA uses `/admin` as the single internal workspace. The sidebar is the only primary admin navigation map, `/admin` is an exception dashboard rather than a duplicate route launcher, Refunds is one shared core `/refunds` workflow, and Scoped Admins can enter with zero machine grants while machine records/actions remain limited to explicitly granted `reporting_machines`.
- Scoped Admin Technician provisioning is tracked by P0 issues `#536`-`#542`; local executable implementation/UAT belongs in `#537`-`#541`, while `#542` requires post-deploy live invite/account verification.
- Operator Pay, partner reporting, and scheduled exports are active shared foundations.
- Operator report PDF exports depend on the deployed `sales-report-export` Edge Function matching the repo's polished generator; stale deployments can still produce legacy monospaced PDFs and should be redeployed before partner-facing report sharing.
- Timekeeping V1 is a distinct worker-entry and machine-manager-review workflow: monthly completed shifts, per-shift whole-hour rounding, worker-visible correction notes, and issued-statement self-service. Shift review does not execute or alter payments.
- Operator Pay calculation, finalization, payment execution, tax/compliance handling, and provider integration remain separate from the Timekeeping V1 replacement for Sheets/AppSheet.
- Operator pay statements slice `#449` remains the versioned foundation for issued-statement self-service; drafts and manager previews are not worker-visible.
- Authenticated portal bootstrap is shell-first and permission-neutral: Technician entitlement resolution remains authoritative before access reads, access-sensitive navigation waits for hydration, and session-to-shell/dashboard timing contains no account or identity data.
- Frontend work should use existing app patterns plus `PRODUCT.md`, `DESIGN.md`, and `impeccable` when the visible experience matters.

## Safety

- Never paste secrets, raw customer data, payment IDs, vendor exports, or free-text complaint content into docs, issues, PRs, or chat.
