# Current Status

Last compacted: 2026-08-27

GitHub Issues and the Bloomjoy Project board are the operational source of truth for active work, priority, blockers, acceptance criteria, and closeout evidence.

## Live Work

- Run `npm run agent:github-hygiene` before planning sprint work, branch syncs, or broad closeout.
- Use issue labels, project status, PR checks, and latest issue/PR comments instead of static docs for task state.
- Keep active blockers, UAT evidence, and closeout notes on the issue or PR where the work is happening.
- Do not add running task chronology, stale PR lists, old branch names, or historical completed-work ledgers here.

## Durable References

- Product and decisions: `Docs/DECISIONS.md`; use `PRODUCT.md` or `DESIGN.md` only when they exist in the repo
- Local setup and verification: `Docs/LOCAL_DEV.md`, `Docs/QA_SMOKE_TEST_CHECKLIST.md`
- Production operations: `Docs/PRODUCTION_RUNBOOK.md`
- Architecture and scope: `Docs/ARCHITECTURE.md`, `Docs/MVP_SCOPE.md`

## Current Themes

### Refund Operations launch snapshot (authoritative as of 2026-08-27)

- **Current operating decision (`#628`, `#990`, `#427`):** use the next genuinely owed, unresolved customer refund of $10 or less as the first direct production proof when one exact Nayax transaction matches amount, currency, machine/account, time, and available card evidence and there is no prior provider attempt or refund. Do not create an employee purchase. Special staffing, observer, recruited UAT, staged cohort, first-ten sampling, and repeated go/no-go ceremony are removed. One actual manager **Refund $X** confirmation remains required. One immutable generation may produce at most one Nayax request and one approval; no bulk action exists; exact authorization, idempotency/unique-stage constraints, caps, circuit breaker, kill switch, audit, and uncertainty hold remain mandatory. #973/#971 automatic readback is nonblocking, with manual Refund Operations verification accepted for ambiguity.

- **Earlier $8 provider attempt is resolved as no refund (`#628`, `#973`, `#971`):** one legitimate manager action created one generation, one request, and one approval. The request returned HTTP `200` without a proven response-contract match; the database's then-current unfamiliar-`2xx` rule authorized the approval; the approval returned HTTP `500`. Bloomjoy created no retry, reporting adjustment, completion, or customer success message. Authoritative DTM evidence later proved no refund occurred, so the case advanced to a new generation without reusing or replaying that attempt. The original attempt remains immutable historical evidence and must never be called again.
- **Retry-safe release projection repair (`#990`):** authoritative DTM evidence confirmed the held $8 transaction was not refunded and advanced the case to a fresh generation without a provider call or customer message. The manager-session resolver correctly preserved the original ambiguous provider facts, but the account circuit breaker and lifecycle projection continued treating those historical facts as unresolved. Migration `20260828035155` recognizes only an exact linked retry-safe resolution whose attempt, case, immutable resolution row, and current generation agree; it reopens that account/case while every unresolved, unknown, stale-generation, or unlinked attempt remains paused.
- **Production runtime contract repair (`#990`):** after the database hold cleared, case availability still failed because `NAYAX_REFUND_MANAGER_CONTRACT_JSON` contained the retired schema-v1 `nayax-production-observed-2026-08-22` shape while the journal-v3 runtime accepts only schema v2. The server now emits one privacy-safe readiness event containing booleans and a case digest, never configuration or credential values. That diagnostic isolated the parser mismatch, the production secret was migrated to `nayax-production-account-contract-v2` without changing the separate write credentials or release scope, and `nayax-card-refund` v97 plus the live portal reached the exact-case **Ready to refund** action before the authorized production proof.
- **Authorized production proof needs response-contract clarification (`#990`):** the manager confirmed one $8 attempt for `RF-E924AA86`. Nayax returned HTTP `200` with valid JSON and the documented `Result`/`Status` string fields, but the pair did not match any account-confirmed accepted state. Bloomjoy therefore made no approval call, recorded the attempt as ambiguous, sent no completion email, created no reporting adjustment, and locked retries. Immediate DTM verification still showed transaction `6037169004` as the original approved sale with blank refund amount and requester, proving no refund request or refund was recorded. This is **not** an invited-user blocker. The canonical active Nayax executor and its provider-confirmed capabilities are recorded in `Docs/DECISIONS.md`; the invited Bloomjoy-email user and an older expired Gmail-email user are not production executors. No user activation or credential move is required by current evidence. Remaining work is to confirm the exact Nayax `Result`/`Status` meaning for the active account/current request token, reconcile the held attempt without retrying it, and complete one newly confirmed live attempt end to end.

- **P0 response/journal v3 repair (`#628`):** the repair candidate requires schema-v2 Bearer configuration, the exact production endpoint, adapter-valid account-scoped credentials, and an exact account-confirmed contract before execution or readiness. Only exact HTTP `200` + `application/json` + valid object/string fields + a recognized accepted pair can authorize approval. The journal stores safe HTTP/media/body/length/schema/semantic classes and a keyed digest, never raw response content or values. The former exact-case calibration switch no longer waives contract or approval-scope proof; journal v2 remains available only for normal-path rollback compatibility, while service-role access to the retired approval-only recovery entrypoints is revoked. Automatic report readback still follows account identity and delivery-contract proof.

- **P1 simple customer intake and secure status (`#993`):** the primary mobile card form now keeps only machine, email, purchase date/approximate time, amount, payment last four or wallet flag, and issue category in the default path; optional diagnostics remain behind one disclosure and the same-case correction loop preserves exact-match safety. A separately default-off 256-bit status capability stores only a digest, uses fragment transport, 30-day expiry, revocation, rate limiting, privacy-safe audit, no-store/no-referrer/noindex controls, and an allowlisted customer-only view of `refund_lifecycle_v1`. Active status refreshes automatically within 15 seconds and terminal status stops. Confirmed copy states that Nayax approved the refund and the bank may need four business days; it never claims bank posting already occurred.

- **P0 durable refund lifecycle (`#991`):** refund matching and provider execution now publish one versioned, redacted lifecycle contract for the current manager session. Read-only Nayax lookup is bounded by time, response size, and record count; a newer deterministic-fact generation supersedes older results. Interrupted work is recovered only as either a proven no-provider-call release or a no-retry confirmation hold backed by the immutable provider journal. Aggregate health names Refund Operations as owner with a 60-minute exception SLA, while browser roles remain unable to read raw provider evidence.

- **P0 definitive no-refund release and live activation (`#990`):** an exact contract-matched Nayax rejection with immutable HTTP-success journal evidence proves no refund occurred. Settlement atomically releases only that consumed attempt as `released_no_refund`, advances the generation, and restores one fresh manager-confirmed **Refund $X** action without reporting or customer-completion effects. Timeout, pending, unknown, transport, journal, settlement, malformed, and unproven rejection outcomes remain locked in Refund Operations. The implementation is deployed; the next eligible unresolved customer case under $10 is the direct operating proof, not a separate non-customer canary.

- **P0 smooth manager lifecycle (`#992`):** the Refunds workspace consumes only `refund_lifecycle_v1`, starts eligible read-only transaction lookup once, refreshes active cases automatically, shows every candidate together with accessible selection semantics, and keeps one **Refund $X** action after server-confirmed selection. Six distinct views separate action, ready, in-progress, Refund Operations, waiting, and completed work. Routine managers never receive provider evidence, credential, or reconciliation controls; permissioned Refund Operations sees the owner, 60-minute SLA, safe stage, and one recovery next step. Synthetic browser UAT completes the exact-match manager journey in under 15 seconds with zero assists and proves one selection request and at most one provider request; no live refund is authorized by this slice.

- **P0 customer-message manager routing (`#945`):** production diagnosis found a denial could save successfully but fail delivery when the customer was also one of the machine's managers. The complete route supports one to four distinct active managers, counts a customer-manager once through the customer To address, copies every other manager, and fails closed for zero managers, malformed mappings, mailbox collisions, or a fifth manager. Denial, follow-up, completion, Gmail, and transactional delivery share the same route evidence and replay-safe behavior.

- **P0 Nayax refund reliability (`#961`, `#971`):** request-to-approval authority lives in an append-only database state machine with an exact Edge/database version handshake, separate account-scoped request and approval credentials, immutable attempt generations, idempotency uniqueness, one-provider-stage enforcement, caps, and an account circuit breaker. Under journal v3, HTTP, response schema, and semantic matching are distinct evidence and only the exact accepted response envelope advances. Unknown/timeout/read/journal/settlement outcomes pause the affected safety scope; provider success, Bloomjoy settlement, and customer delivery remain distinct phases. Historical Tulsa reconciliation proved a separate negative Nayax refund can exist while the original sale remains unchanged, so ambiguity is checked in Nayax before another action. Automatic report-feed readback is a P1 improvement, not an activation dependency; the structured manual Refund Operations path is the accepted fallback.

- **P0 Nayax Last Sales matching (`#961`):** live Tulsa canary preparation exposed that Nayax's documented machine Last Sales response omits a separate payment-status field. The previous matcher treated that documented omission as unconfirmed and downgraded an otherwise exact machine, amount, time, card, network, and currency match to manual review. The repair binds omission-as-sale evidence only to the explicit Last Sales contract; a present blank, unknown, or negative status and every unverified payload remain fail-closed.

- **Simple Card/Cash refund paths (`#958`, `#959`):** the owner decision is Card via the existing guarded Nayax API first, and Cash via a manager-arranged external payment until gift cards are available. The public form now starts with plain **Card** and **Cash** choices, keeps the existing limited card evidence only for Card, and submits Cash with ordinary case facts only—no card fields, payout handle, or Nayax request. The one ambiguous two-machine location asks only for the visible TT machine label. Every active cash case with a valid stored amount then has one **Mark $X as refunded** action; the server derives amount, actor, and time, atomically records one `manual_external` adjustment and redacted official event, sends one channel-neutral completion email after commit, and replays without duplication. Gift-card fulfillment remains future `#666` work.
- **Refund simplicity recovery (`#946`-`#949`):** transaction confirmation is row-locked and replay-safe, the manager receives authoritative readiness, and one **Refund $X** primary action appears after exact selection. The server rechecks mapping, authorization, amount, evidence freshness, machine and daily caps, duplicates, runtime configuration, idempotency, and account health. All 34 qualified machines are enabled under the reviewed $50 machine cap. The private provider ledger exposes only service-role aggregate daily count and amount totals to readiness checks. The next direct customer refund and the following 72-hour observation own operational closeout; recruited UAT is not a launch gate.

- **Integrated v1 release (`#628`):** form-only intake (`#889`), Nayax inventory (`#890`), branded messages and same-case appeals (`#891`), duplicate protection (`#703`), and the unified source-aware queue (`#706`) are merged. The Bloomjoy form is live. A production incident found that public intake readiness had been separated from read-only transaction-search readiness, so a customer could submit successfully and then see **Transaction search unavailable**.
- **Form and email-assistant boundary (`#889`):** customer contact records no refund case and sends one warm Bloomjoy form response only when the response lane is later enabled. The one-time Email-linked context or a direct Website form submission creates exactly one case. Verified replies add only changed safe facts to the same case and rerun matching; replays cannot create a competing actionable case.
- **Nayax production incident recovery (`#890`):** the nine exact one-to-one repairs are deployed. The two affected Great Mall cases left **Transaction search unavailable**: one correctly stopped for manager comparison and one moved to the reply-based wallet-detail follow-up. Both retained zero decisions, zero official-action authorizations, zero provider refund attempts, and payment-ineligible state.
- **Remaining active-inventory reconciliation (`#890`):** a reviewed follow-up migration covers all 22 remaining active provider rows: 16 real customer-facing machines receive exact provider identities, customer-safe locations/timezones, public form labels, and the existing bounded manager route; `Snapcase 03` and `BS08 Popcorn Christiana mall` remain explicitly **Needs setup**; four unmistakable test/invalid names are explicitly **Excluded**. The expected production inventory becomes 33 Published, 2 Needs setup, 4 Excluded, and 0 unaccounted, with no payment enablement change. Thirteen older live-form locations are absent from the only connected Nayax feed and therefore remain available for intake but lookup-blocked until a real provider identity appears; no unrelated location is substituted.
- **Temporary Adam-managed NC path (`#890`):** twelve customer-facing machines on Adam's Nayax account remain without Bloomjoy API identities. A reviewed temporary path lets the exact mapped manager enter one safe transaction from the Nayax portal, separately confirm it, separately approve the refund, complete it once in Nayax, and then record the exact Nayax confirmation through the existing result-resolution boundary. The path never invents a site or machine ID, never calls Nayax, blocks account-wide duplicate transaction references, and creates no reporting adjustment or customer email until confirmed completion. Atlanta remains on the automatic feed; Oscar's Playground remains excluded from this Adam-only cohort. Gmail-origin completion stays on the original `info@bloomjoysweets.com` thread and Website completion keeps `Reply-To: info@bloomjoysweets.com`.
- **Manual-path timezone hotfix (`#890`):** live verification found that all twelve API-pending machines inherited `America/Los_Angeles` from their one shared legacy placeholder location. A narrow follow-up stores the reviewed machine-local timezone directly on each temporary manual-path machine (eight Eastern and four Central), makes the UI and exact transaction conversion use that value, and fails closed on a missing or invalid timezone. It does not alter the shared placeholder, manager authority, transaction/approval separation, payment execution, duplicate scope, reporting, or customer communication.
- **Refund location-selection stabilization (`#921`):** production validation found that grouping only by a legacy placeholder location UUID omitted 15 reviewed customer labels. The repair groups on the customer-safe label too, restoring distinct choices such as Bubble Planet Atlanta/DC/Seattle, Carolina Place, and Columbiana Centre without inventing Nayax mappings. Every ordinary choice remains one-to-one, South Hills remains separated by explicit cotton-candy/SnapCase category, and only the reviewed two-machine Livermore pair is compressed. The submitted opaque scope remains separate from exact machine identity; transaction confirmation must still bind the exact machine before the separate approve/deny/refund path. Production activation remains gated on reviewed merge, release alignment, and full postdeploy smoke/UAT.
- **Customer messages and appeals (`#891`):** one canonical warm Bloomjoy email system covers first contact, missing-information follow-up, denial, appeal receipt, retry, and confirmed completion. A verified reply after a sent denial reopens the same case as **Appeal needs review**, clears the prior decision, and cannot authorize or attempt payment. Automatic appeal receipts remain independently default-off and stop on uncertain delivery.

- **Current manager workflow:** the reviewed simplification replaces refund-specific TOTP and temporary operator ceremony with the signed-in exact Machine Manager session. The page now has distinct **Action needed**, **Ready to refund**, **In progress**, permissioned **Needs Refund Operations**, **Waiting**, and **Done** views, plus search, one plain lifecycle, and one next action. Routine system-health banners, authenticator setup, six-digit-code dialogs, provider evidence, advanced status controls, and repeated warning boxes remain out of the manager path. Server-side mapping, case-version, payload, row-lock, idempotency, cap, duplicate-payment, reporting, and original-thread email controls remain unchanged.
- **Mapped-manager denial (`#918`):** after confirming an exact card transaction, the manager must still see a visible secondary **Deny request** path. Denial uses one server-validated customer-safe reason, sends no Nayax request, creates no payment/reporting adjustment, and keeps reply-based appeal on the same case.
- **Customer email transport (`#917`):** customer-visible mail uses the display name **Bloomjoy Refunds**. Gmail-linked cases stay in the original `info@bloomjoysweets.com` thread; direct Website cases keep the verified transactional sender and `Reply-To: info@bloomjoysweets.com`. Delivery uncertainty never falls through to a second conversation, and manager-only notices remain separate.
- **Transaction-result clarity:** the manager workbench states how many search results are currently selectable, shows the closest transaction first, and collapses the remaining results. A physical-card mismatch names the conflicting card evidence and reports zero selectable transactions. A case waiting for a customer reply shows its current result only as context and keeps selection disabled until the reply triggers a fresh search. Matching, duplicate-payment, and refund-execution controls are unchanged.
- **Card-network evidence (`#923`):** card intake and the single-use customer correction collect Visa, Mastercard, Discover, American Express, or Other / Not sure without collecting full card data. The manager compares the customer value with normalized Nayax card type in plain language. A match is supporting evidence; a physical-card mismatch keeps that candidate unselectable; wallet differences remain supportive only. Network evidence never selects a transaction, approves a refund, changes the exact selected Nayax amount, or bypasses duplicate/payment controls.
- **Customer-correction email defect (`#924`):** the immediate read-only Nayax check and scheduled email assistant previously stopped at separate boundaries, and physical-card conflicts were classified as manager-only work before the customer-information action. The repair makes the assistant act once on both fresh and already-persisted no-safe-match results, requests only the customer-correctable time/payment/amount/physical-card facts, keeps provider/setup/duplicate/already-refunded evidence manager-only, and lets a verified labeled reply correct the same case and rerun matching. It does not select a transaction, approve a case, or call a refund endpoint.
- **Unified refund queue (`#706`):** every form-created case stays in the same three manager queues and carries a compact **Support email** or **Website form** source beside its reference on desktop and mobile. The source label is informational only; case state, next action, manager scope, duplicate review, and payment safeguards are unchanged.
- **Held $10.90 case:** production closeout is complete. Nayax support confirmed the existing transaction was refunded, Bloomjoy committed that provider-free result exactly once, and the guarded website-form recovery sent the saved customer email once at 2026-08-21 09:18 UTC. The final state has one successful attempt, one outcome resolution, one reporting adjustment, one sent completion message, one email-only retry, and no second provider call. The same customer/manager address was correctly treated as one recipient instead of a duplicate CC.
- **Routine Nayax production lane:** the canonical active account, requested Lynx roles, and separate production request/approval tokens are confirmed; the invited and expired duplicate users are not dependencies. No further provider payment action is ready until Nayax's exact `Result`/`Status` semantics for the active account/current request token are confirmed and the held attempt is reconciled without retry. After that proof, only a database-authorized exact HTTP `200` JSON contract match may advance once; every other request or approval result remains on a reconciliation hold. A later proof still uses a legitimate eligible customer case under the $10 operating limit, never a synthetic purchase. The runtime kill switch, exact case release scope, per-refund/machine/daily caps, exact transaction binding, idempotency, duplicate-payment controls, append-only journal, and account circuit breaker govern execution. Missing automatic readback does not block manual authoritative reconciliation.
- **Temporary launch limits:** production is capped at $50 per refund, $500 in total approved refunds per day, and 20 approved refunds per day. These are monitoring guardrails, not permanent product requirements. Transaction confirmation and refund approval/denial remain separate manager actions, and activation started with zero pending approvals, step-ups, or unresolved provider attempts.
- Older TOTP/operator/setup bullets below are retained as historical audit context only. They are not current manager requirements where they conflict with the 2026-08-20 manager-session decision in `Docs/DECISIONS.md`.

- The next manager-flow slice removes confidence as an execution gate after explicit transaction selection. Bloomjoy keeps the existing deterministic candidate ordering and hard exclusions, shows every safe candidate with the most likely first, and lets the mapped manager confirm the correct one. Wallet-token card digits and a different customer-reported amount remain comparison clues; the exact selected Nayax transaction amount becomes the refund amount. The selection audit, mapped-manager authority, exact transaction binding, caps, duplicate protection, idempotency, and provider-result handling remain unchanged.

- P0 `#630` connects the existing case fact-version/readiness transition to the existing Nayax Last Sales lookup. A lookup-ready card case now claims one automatic read-only lookup per deterministic evidence version during hosted intake/private email-linked completion or customer-reply recheck; unchanged page opens/renders do not trigger it. The existing deterministic recommendation, sanitized candidates, and plain-language reasons remain the manager result, while ambiguous, unmatched, setup, and failed cases stay open with a manual **Refresh transaction results** fallback. This slice does not call the Nayax refund adapter or enable payment execution.
- P0 `#631` / `#758` present the existing refund, lookup, provider, and customer-message facts as one plain manager state with one next step. The routine matched-card header shows **Ready for review**, separate **Payment** and **Customer** badges, the existing Nayax recommendation/reasons, and one **Refund $X** action. Needs-information, automatic-checking, ambiguous/unmatched, rejected, uncertain-result, completed, and denied states stay distinct without adding a second workflow or changing provider execution. Manager-facing copy now says what happened and what to do next without exposing internal terms such as bounded workflow, correlation evidence, provider hold, or backend guardrails; raw lookup messages are mapped to concise manager notices before display. The final manager review also keeps the queue and selected case on the same current task, uses **Confirm this transaction** for evidence selection, and removes duplicate disabled-action warnings from transaction-review states.

- The P0 `#430` database and Edge release is deployed, with hosted Auth still enrollment-off/verification-on and Gmail, GPT, automation, and broad customer contact closed. The first owner acceptance was deliberately stopped before any provider call when the non-financial candidate-selection save reached an older official-action guard. The dedicated selection boundary in this release fixes that path without authorizing payment: it requires the exact mapped manager, fresh case revision, and actor-bound private lookup token; keeps the case in `needs_review` with no decision; binds the refund amount to the selected Nayax amount; and atomically records one redacted selection event. The halted attempt produced zero provider attempts, customer messages, adjustments, completions, or official authorizations.
- Before the August 21 inventory regression, the live hosted refund form exposed **29 active, customer-safe Commercial/Mini portfolio locations**. The corrected selector targets those 29 plus the explicitly represented Great Mall Snapcase location for **30** current options; exact post-deployment desktop/mobile evidence is required before activation resumes.
- The owner-controlled, case-specific Gmail proof passed once through the designated `info@bloomjoysweets.com` mailbox: exactly one case message and one Gmail outbound in the original thread, with the complete current mapped-manager CC route, zero attachments, and zero unresolved outbound afterward. Teardown restored the authorization and every unrelated gate to off. This was synthetic proof only; the legacy responder remains authoritative for normal customer mail.
- The dedicated candidate-selection hotfix is deployed and the exact $10.90 East Ridge transaction was saved with one redacted selection event and zero payment/customer side effects. The live acceptance then failed closed before the refund button because the scoped overview still inherited the broad official-action hard-off gate. The follow-up overview boundary exposes only the already-reviewed mapped-manager, selected-transaction Nayax path while keeping unrelated official actions disabled. The single owner-authorized $10.90 East Ridge acceptance in `#427` remains the last bounded action, with only that machine and exact amount/count caps opened for the execution window. Email cutover (`#707`, `#409`), unmatched/cash fallback (`#666`), and production drift monitoring (`#768`) remain separate work.
- The follow-up overview boundary is deployed. The single owner-authorized $10.90 acceptance reserved exactly one provider attempt, but Nayax returned an unrecognized request-stage response, so the attempt is durably `ambiguous` with `provider_request_outcome_unknown` and reconciliation required. Bloomjoy did not retry, create a reporting adjustment, finalize the case, or send a customer completion email. Runtime execution, the machine allowlist/cap, and the dedicated provider caller were restored to closed immediately. The case remains in **Check refund result** until the Dynamic Transactions Monitor or Nayax support confirms whether the provider created a pending refund request.
- Nayax support confirmed that exact transaction `6841061866` appears **refunded** on its side. Support described missing user-level roles only as a possible explanation for the earlier approval error; it did not identify a required role or say a new grant was necessary. That historical attempt was reconciled exactly once and must never be called again. Routine production readiness now relies on the successful refund evidence and fail-closed provider outcomes, not speculative permission work.
- P0 `#877`'s historical recovery records remain deployed for audit and rollback compatibility, but the approval-only runtime operation is retired fail-closed. Journal v3 cannot authorize a standalone approval from incomplete legacy request evidence. The current $8 attempt and every other authoritative-unknown attempt therefore remain provider-free reconciliation work; no recovery call may create another approval. P0 `#427` retains the reviewed structured-resolution mechanism for authoritative DTM/support evidence without a provider write.
- Scheduled automation, Gmail sync/retention, and GPT triage remain disabled. The existing refund-adjustment sync is an intentional reporting-ingestion exception and remains enabled; it does not authorize customer contact, an official action, or a Nayax refund.
- P0 `#854` delivered the deployed, default-off owner-only Gmail intake-shadow foundation. Live execution never changes project secrets: it requires a fresh exact database dispatch authorization, one bounded thread, one POST with no retry, terminal reconciliation, and a conclusively closed authorization. Its then-current `10/51` release was inert pending the owner-private ceremony; that count and activation statement are historical and do not define the current Nayax refund lane. Gmail sync, delivery, retention, and schedules remain independently off.
- P0 `#430` supersedes the earlier owner-only pilot ceremony for the normal product path. Normal execution uses the signed-in current Machine Manager session—no sponsor packet, written-provider-approval dependency, private TOTP ceremony, or temporary pilot credential. Provider credentials remain server-side, and the ordinary kill switch, enablement flag, dry-run flag, exact evidence/version checks, machine allowlist/cap, daily caps, assertion-scoped reservation, and one-attempt settlement still fail closed.

The detailed refund bullets below retain supporting design and audit context. Where an older dated count or pre-proof checkpoint differs, this snapshot governs.

- **Legacy refund-state truthfulness (`#784`, `#793`):** production normalization is complete. The database-owner-only repair normalized exactly one eligible legacy case to `needs_review`; cleared its current decision, selected match, recommendation, and lookup candidates; and preserved both historical sent messages unchanged. Sanitized post-checks recorded zero provider attempts or calls, zero new customer messages, zero reporting adjustments or completion records, and `officialActionsEnabled=false`. Hosted Auth remained enrollment-off/verification-on before and after the operation, the temporary token was revoked, and the current release now aligns to ten reviewed Refund Operations functions and 51 refund/Nayax migrations. The case remains frozen from official action until a fresh read-only transaction check.
- **Historical refund-readiness checkpoint (superseded):** at that checkpoint, the strict Nayax adapter and live execution were still unreachable and fresh TOTP was required. Those activation statements are not current; the 2026-08-27/28 launch snapshot and newest decisions govern. The exact-machine manager authority, caps, duplicate protection, and no-retry handling remain current safety controls.
- **Operational refund email facts:** verified customer Gmail messages can add explicit, labeled routine facts to the linked case. During an active no-safe-match correction cycle, those same strict labels can correct the linked case's machine/location, date/time, amount, payment method, or physical-card last four before the existing fact-version lookup reruns. Machine/location text must resolve to exactly one active customer-facing machine label; prose, quoted history, ambiguous labels, wallet digits, sensitive payment content, and legal/safety/threat/chargeback language are not guessed and remain manager work. This does not alter transaction selection, approval, payment, or duplicate protections.
- **Launch boundary:** exact OAuth for the directly connected production customer-service mailbox, `info@bloomjoysweets.com`, with Gmail read-only/send scopes; verified Info/Support/Refunds send-as identities; isolated exactly-once first contact; replay suppression; and private-form continuation all passed. P0 `#810` delivered the required owner-only one-command runner, and P0 `#814` adds the explicit owner-grade Management API database path with a fixed SQL allowlist. That runner completed one case-specific synthetic proof from exact `info@` in the original Gmail thread with the complete current mapped-manager CC route: exactly one case message and one Gmail outbound, zero attachments, zero unresolved delivery, and every gate restored off. This did not enable normal mail processing. The legacy responder remains authoritative, broad Gmail polling/customer contact remain off, the approved 180-day sanitized-copy retention schedule remains dormant, and EasyText/SMS remains outside this pilot.
- Payment-first storefront policy is tracked in P0 `#715`: the Commercial Machine is the only quote/request product; available sugar, Bloomjoy branded sticks, Micro Machine, and Plus checkout before Ethan/Ian and WeCom alerts. Mini and custom sticks remain visibly unavailable until their complete payment-first pricing and fulfillment rules are ready. Micro keeps its `$2,200` planning price but checkout is client- and server-gated off by default until the open executive shipping decision in `#717` is resolved; it never falls back to an unpaid request. California Stripe Tax launch approval is tracked separately in P0 `#718`. The CDTFA seller's permit is current from 2026-01-01 with a yearly period ending 2026-12-31; Stripe's live and sandbox head-office records match the privately confirmed Mountain View location, live Sugar products use `txcd_40020004`, and filing/remittance ownership is documented in `Docs/SALES_TAX_OPERATIONS.md`. Live California collection was owner-approved and activated on 2026-08-08 at 16:58 UTC. The reviewed Sugar, branded-sticks, and Plus checkout creators were deployed at 17:18:21 UTC; live no-payment evidence confirms Automatic Tax for Sugar and sticks, California Sugar exemption, positive California sticks tax, and no collection for a no-registration destination. The live Stripe Plus product description matches the approved flat `$100/month` per-customer-account model without changing its price, Price reference, or tax configuration. PR `#716` is merged and its reviewed frontend was promoted to the production domains after clearing a stale Vercel Instant Rollback. It adds the portal-side authenticated checkout action, resolves every known Stripe Customer and Plus subscription before acting, treats Stripe as authoritative over stale local subscription state, rejects conflicting Customer or subscription ownership metadata, fails closed on ambiguous histories, resumes an existing incomplete Checkout instead of sending it to Billing, and uses a durable per-account creation lease plus Stripe idempotency to prevent concurrent or timeout-retry duplicate Checkout Sessions. Post-deployment no-payment smoke identified one additional recovery boundary: explicitly expiring a diagnostic Session before its original Stripe `expires_at` left the durable `ready` row reusable. The forward-only `202608100001_plus_expired_checkout_recovery.sql` migration and matching `stripe-plus-checkout` recovery path make the provider's no-open-session result authoritative, release only the signed-in account's matching stale row, and preserve one-session concurrency protection. Failed-payment recovery, scheduled-cancellation renewal, terminal-subscription restart, duplicate fallback, incomplete-checkout recovery, early-expiry recovery, and billing-refresh failure are covered by database, unit, persona, and authenticated no-payment UAT. The billing portal uses an explicit reviewed Stripe configuration with payment-method updates, invoice history, and end-of-period cancellation/renewal. Shipping treatment and the best Bloomjoy Plus product-code mapping remain documented working positions. Release acceptance requires zero open diagnostic Checkout Sessions, no payment/order/subscription/entitlement/communication side effects, clean Stripe ownership, the reviewed tax/webhook behavior, and both client/server Micro gates remaining off.
- **Historical 10-function/51-migration checkpoint (superseded):** this older release count and its TOTP/default-off activation state are retained only as audit history. Current release counts and gates come from the release manifest and the launch snapshot above.
- **Historical Machine-QR checkpoint (superseded in part):** QR evidence remains matching evidence only and cannot approve a refund. Its fresh-TOTP and supervised-pilot requirements are retired for the normal manager path; current manager authority and provider gates follow the newest decisions.
- P0 `#750` is merged, owner-approved, and deployed to production. The hosted form records customer-described payment interaction, wallet provider, time accuracy, and issue category without treating those statements as Nayax facts. Follow-up `#753` removes the redundant customer product/selection question because the selected machine determines whether the purchase is a phone case or cotton candy; legacy stored product notes remain readable. Matching policy `2026-08-11.v3` keeps rough or hour-wide customer times in manager review; optional Nayax product, configured-price, current-status, and nearby-alert context is read-only, fail-soft, and does not add ranking points. The manager workbench presents one customer-versus-machine comparison, plain-language reasons, collapsed alternatives/context, and one primary manager action. Routine system health stays out of the task; only actionable delivery/reminder warnings appear. Route, portfolio-option, and Nayax-mapping smokes pass with live Nayax execution and all optional assistant schedules still disabled.
- The approved refund target is one manager decision, not a manual Nayax workflow (`#674`). Bloomjoy should automatically attempt matching, use the secure customer wallet-detail correction loop in `#673` when useful, and show the manager one **Approve refund** or **Decline** decision only after one safe transaction is identified. Confirmed provider success should create one original-thread customer completion message with the complete current mapped-manager set visibly CC'd; it must not send a separate duplicate manager completion email.
- The `#673` wallet-correction slice is merged behind default-off customer-contact controls. A wallet no-match may receive one 48-hour single-use correction link plus at most one replacement reminder; the form accepts only wallet type, virtual/device last four, approximate local purchase time, and amount confirmation. Submission preserves the machine and QR evidence, automatically re-runs deterministic Nayax matching, notifies managers only for a high-confidence transaction, and otherwise marks the case eligible for the still-TBD fallback route. Raw link tokens and forbidden payment credentials are not stored.
- **Historical 10-function/51-migration verification (superseded):** these counts and the associated safe-foundation-only conclusion describe an older release. Use the current release manifest and current PR checks for live counts.
- **Historical 29-location/zero-enabled snapshot (superseded):** this inventory and activation state is no longer current. The launch snapshot above records 34 qualified machines enabled under the reviewed cap; special TOTP and shadow-cohort setup are not present blockers.
- **Historical checkpoint (superseded by the 2026-08-27/28 launch snapshot):** live execution was previously sponsor-gated behind `#430`/`#767`, private TOTP, and a special cohort. Those launch ceremonies are no longer current blockers. Their provider-call safety, exact evidence, caps, idempotency, and reconciliation controls remain, while the current blocker is the active account's exact response semantics and a fresh end-to-end proof after the held attempt is reconciled.
- Alternative compensation is now a P0 dependency, but its provider remains undecided (`#666`). It is offered only after bounded matching/customer-correction attempts reach a terminal unmatched state, or for ineligible payment methods such as cash. An unknown provider outcome must be reconciled before compensation so one claim cannot receive both remedies.
- `info@bloomjoysweets.com` is the directly connected production customer-service mailbox. Its verified Info/Support/Refunds send-as identities remain identities of that mailbox, subject to provider `SENT` evidence and every existing delivery gate. `etrifari@bloomjoysweets.com` and its plus-addresses may be used only as owner-controlled synthetic customer/test identities or for vendor correspondence; they are not the production refund-assistant mailbox. Attachment-free operation, the 180-day sanitized-copy policy, first contact, private-form continuation, and one case-specific original-thread reply with complete current mapped-manager CC have passed. The remaining email gate is the staffed production-label/legacy-responder cutover and explicit owner go/no-go in `#707`/`#409`.
- Refund intake ownership now uses one database rule for direct website submissions and private email-linked form completion: exactly one current active mapping is assigned under the Admin Machines lock; zero or multiple mappings remain unassigned for explicit review. The release also repairs eligible existing open unassigned cases with a redacted audit event, while all Gmail, automatic contact, official action, and Nayax switches stay off.
- The exact current machine mapping is the sole official-action grant. Admin access alone cannot act, while a mapped manager who also holds Super Admin or Scoped Admin access remains valid only through that mapping. Production verification passed for the selected mapped dual-role owner/case, but the global official-action gate remains false.
- Automation scheduler credentials and endpoint plumbing are configured without enabling the lane. A PII-free forced-failure alert, exact-key replay suppression, and a disabled-lane no-op passed in production-safe runs. `#632` still needs one staffed synthetic due reminder/escalation, manager-visible healthy state, replay, and teardown; GitHub/Edge automation and manager-aging switches remain false.
- Fresh action-bound TOTP and receipt protections are implemented, synthetically verified, and deployed default-closed. P0 `#782` is merged through `#785`/`#787` with a private immutable Auth-UUID digest (not an email hash), lock-time owner/admin/confirmed-identity/mapping revalidation, one self-only five-minute database window, and expiry/cancel/success cleanup with sanitized audit. Hosted Auth is restored to enrollment-off/verification-on, and P0 `#789`'s exact-project, GET-only live closed-state gate passed immediately before and after the two refund migrations and two manager functions were deployed. No enrollment window was opened, no QR/code was exposed, and official payment, Nayax, Gmail, customer-contact, GPT, automation, and manager-aging gates stayed off.
- Scheduled production drift remains a release blocker in `#768`: owner-controlled local and strict read-only production comparison passes ten functions and 51 migrations, but GitHub must not receive a broad owner PAT. The monitor stays fail-closed until a Bloomjoy-project-only Edge Functions Read credential is privately created, validated, stored in the protected environment, and proven on `main`.
- GPT triage remains default-off and human-review-only. The server runner, strict schema, content-free job ledger, and local developer credential destination are implemented; no production OpenAI secret is configured. A new fail-closed server acknowledgement prevents provider execution until `#635` records the exact OpenAI project retention/data-control mode and privacy/security approval; `store=false` is not treated as zero-retention proof. The database structurally prevents automatic sending.
- Refund operations and customer-refund pilot readiness remain high-sensitivity operational surfaces.
- Access grants that create user-facing Corporate Partner or Technician access must prove both the saved grant and the invite-email attempt; rollout signoff also needs provider/inbox/activation evidence in the linked issue or PR.
- Partner activation uses manual Email Codes and a temporary non-persisting auth session; password creation must succeed before portal sign-in. Hosted signup-confirmation, invite, Email Code, and recovery templates must pass `npm run auth:templates:validate` so email-security prefetch cannot consume credentials. Production Auth delivery uses Bloomjoy's Resend custom SMTP; Supabase's two-email/hour demonstration sender is not an acceptable production fallback.
- Technician management IA uses `/portal/team` for Plus Customer and Corporate Partner self-service, while `/admin/access` remains the Super Admin override surface and the Scoped Admin machine-scoped Technician grant surface. Plus/partner users should not receive `/admin` just to manage Technicians.
- Admin Console IA uses `/admin` as the single internal workspace. The sidebar is the only primary admin navigation map, `/admin` is an exception dashboard rather than a duplicate route launcher, Refunds is one shared core `/refunds` workflow, and Scoped Admins can enter with zero machine grants while machine records/actions remain limited to explicitly granted `reporting_machines`.
- Scoped Admin Technician provisioning is tracked by P0 issues `#536`-`#542`; local executable implementation/UAT belongs in `#537`-`#541`, while `#542` requires post-deploy live invite/account verification.
- Operator Pay, partner reporting, and scheduled exports are active shared foundations.
- Operator report PDF exports depend on the deployed `sales-report-export` Edge Function matching the repo's polished generator; stale deployments can still produce legacy monospaced PDFs and should be redeployed before partner-facing report sharing.
- Timekeeping V1 is a distinct worker-entry and machine-manager-review workflow: monthly completed shifts, per-shift whole-hour rounding, worker-visible correction notes, and issued-statement self-service. Shift review does not execute or alter payments.
- Operator Pay calculation, finalization, payment execution, tax/compliance handling, and provider integration remain separate from the Timekeeping V1 replacement for Sheets/AppSheet.
- Operator pay statements slice `#449` remains the versioned foundation for issued-statement self-service; drafts and manager previews are not worker-visible.
- Authenticated portal bootstrap is shell-first and permission-neutral: Technician entitlement resolution remains authoritative before access reads, access-sensitive navigation waits for hydration, and session-to-shell/dashboard timing contains no account or identity data.
- Frontend work should use existing app patterns plus `PRODUCT.md`, `DESIGN.md`, and `impeccable` when the visible experience matters.

## Safety

- Never paste secrets, raw customer data, payment IDs, vendor exports, or free-text complaint content into docs, issues, PRs, or chat.
