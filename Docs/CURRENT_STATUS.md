# Current Status

Last compacted: 2026-07-26

GitHub Issues and the Bloomjoy Project board are the operational source of truth for active work, priority, blockers, acceptance criteria, and closeout evidence.

## Live Work

- Run `npm run agent:github-hygiene` before planning sprint work, branch syncs, or broad closeout.
- Use issue labels, project status, PR checks, and latest issue/PR comments instead of static docs for task state.
- Keep active blockers, UAT evidence, and closeout notes on the issue or PR where the work is happening.
- Do not add running task chronology, stale PR lists, old branch names, or historical completed-work ledgers here.

## Durable References

- Product and decisions: `Docs/DECISIONS.md`; use `PRODUCT.md` or `DESIGN.md` only when they exist in the repo
- Local setup and verification: `Docs/LOCAL_DEV.md`, `Docs/QA_SMOKE_TEST_CHECKLIST.md`
- Production operations: `Docs/PRODUCTION_RUNBOOK.md`
- Architecture and scope: `Docs/ARCHITECTURE.md`, `Docs/MVP_SCOPE.md`

## Current Themes

- Refund Operations PR `#644` merged and the sponsor-authorized safe core deployed on 2026-07-22. Production now includes all 23 required refund migrations, eight reviewed Edge Functions, deterministic Nayax recommendation, the simplified manager card/cash workbench, automation health, Gmail thread linkage, and the human-review-only GPT triage runner. Live Nayax execution, automation schedules, Gmail, and GPT remain disabled; the legacy Google Form/Sheet/AppSheet workflow remains the fallback.
- Machine-QR refund intake (`#662`) is merged, and the source matcher now uses policy `2026-07-26.v2` (`#663`) to keep customer-reported time separate from verified QR-open time. It can recommend either a strong card match or exactly one machine/amount/QR/time match; no recommendation is delivery-failure proof or refund approval. The current unique QR/time path remains manual-only until the new `#674` execution eligibility is implemented and tested.
- The approved refund target is one manager decision, not a manual Nayax workflow (`#674`). Bloomjoy should automatically attempt matching, use the secure customer wallet-detail correction loop in `#673` when useful, and show the manager one **Approve refund** or **Decline** decision only after one safe transaction is identified. Approval should execute the server-side Nayax flow and send one customer-facing confirmation with the send-time active mapped Machine Managers copied; it must not send a duplicate manager completion email.
- The `#689` Machine Manager official-action boundary and `#692` per-action human TOTP step-up are implemented for review, not production-enabled. An eligible manager first reviews one exact action, then receives a two-minute, single-use intent bound to the actor, case/action/target, manager mapping and case versions, durable owner-targeted refund-factor enrollment version, amount/decision payload, and immutable evidence. Nayax execution additionally freezes the persisted transaction match plus locked machine/provider configuration and revalidates that fingerprint again at service consumption. After Auth verifies the exact owner-approved factor, the trusted Edge flow mints a random one-use proof whose digest is bound to the intent; direct target calls without that proof fail even with a generic AAL2 token. A newly entered TOTP proof strictly newer than that intent may mint one 30-second receipt; generic pre-existing factors, same-second, stale, refresh-only, replayed, drifted, agent, service, unrelated, revoked, Super Admin, and Scoped Admin paths fail closed. Enrollment state and sanitized audit evidence commit atomically; a failed recording triggers best-effort durable revocation and Auth-factor removal. The production protocol gate remains hard-coded false, Supabase Auth enrollment and the database owner-targeted enrollment window both default closed, and raw codes, factor IDs, one-use proofs, secrets, QR material, JWTs, customer/payment payloads, and recipient addresses are excluded from audit records and application logs. Activation still requires owner-supervised manager-only enrollment with no QR screenshots, recovery ownership, privacy/security review, and owner UAT before a separate reviewed migration can replace the hard-off gate.
- The `#673` wallet-correction slice is implemented for review but is not production-enabled. A wallet no-match now receives one 48-hour single-use correction link plus at most one replacement reminder; the form accepts only wallet type, virtual/device last four, approximate local purchase time, and amount confirmation. Submission preserves the machine and QR evidence, automatically re-runs deterministic Nayax matching, notifies managers only for a high-confidence transaction, and otherwise marks the case eligible for the still-TBD fallback route. Raw link tokens and forbidden payment credentials are not stored.
- The merged release is green across 115 disposable-project migrations and 209 database tests, including 54 GPT runner safety assertions, plus deterministic matcher, payment-safety, mocked OpenAI failure-path, full Refund portal browser UAT, Machine Manager UAT, and release/rollback validation. The production route and location smokes pass, all eight deployed source bundles match the reviewed manifest, and the live customer form renders without browser errors on desktop and mobile for both card and cash/Zelle states. The guarded synthetic email smoke was not executed because an owner-controlled test inbox and privately approved machine were not supplied.
- Production has six non-duplicate public options, no internal `Unmapped`/`Unknown` labels, and at least one Atlanta, DC, and Seattle option. The aggregate access audit still finds 25 active Machine Manager assignments across three identities but no clean manager-only identity; existing identities overlap with broader access. Clean-account setup, manager-only production UAT, controlled provider pilots, and the named shadow pilot remain pending.
- Live Nayax execution remains disabled and sponsor-gated. Nayax's public documentation confirms a two-step refund request/approval flow and documents `SiteID` in Last Sales, but Bloomjoy's captured production response omitted that field and the existing token was obtained for reporting. Before the one-approval target can execute a live refund, `#430` must confirm production endpoint/token write authority, amount and response semantics, idempotency/reconciliation behavior, a production `SiteID` source, and explicit approved-sale evidence (or approved substitutes), then record caps, allowlist, and controlled low-value smoke approval.
- Alternative compensation is now a P0 dependency, but its provider remains undecided (`#666`). It is offered only after bounded matching/customer-correction attempts reach a terminal unmatched state, or for ineligible payment methods such as cash. An unknown provider outcome must be reconciled before compensation so one claim cannot receive both remedies.
- Gmail remains disabled and unconfigured in production. The sponsor designated `info@bloomjoysweets.com` for `#634`; designated-mailbox OAuth/authorization, alias and label/filter ownership, secure server-only credentials, recipient-boundary/privacy approval, retention/quarantine approval, integrated synthetic UAT, and explicit rollout approval remain release gates. The shared outbound transport now treats `REFUND_GMAIL_ENABLED=false` as a redacted zero-call shutdown: a Gmail-linked delivery stops before its database claim, OAuth refresh, or Gmail request even when credentials remain configured, while hosted-form intake, non-Gmail delivery authorization, and portal review remain available. The default-off `#688` first-contact slice adds one durable acknowledgement claim per Gmail thread, no-send shadow and isolated-test modes, original-thread loop-suppressed delivery, deterministic customer-first copy, fail-closed cutover/rollback controls, and fair, lease- and attempt-version-fenced Message-ID reconciliation for uncertain first-contact or manager replies. Any unresolved delivery keeps Gmail health failed and blocks every reply path; a genuine no-send can be cleared only by an authorized, audited portal confirmation backed by the latest completed versioned zero-result search.
- The participant-safe `#686` slice is implemented for review: a configured mailbox alias is trusted as mailbox-origin only with Gmail `SENT`-label evidence, only verified direct customer replies may change customer state or reach GPT triage, and current active machine mappings are re-resolved before every send. The exact case customer outranks a conflicting manager-mapping row. Every customer-facing Gmail or transactional refund message, including first contact, requires one to three valid current managers in visible CC; unresolved machines, zero-manager routes, empty filtered results, and invalid/over-cap mappings fail closed before provider delivery. That deliberate ownership/privacy tradeoff can delay even the email asking which machine was used until routing is repaired. Automatic contact pauses case-wide only when an `mx.google.com` receiver result authenticates the claimed Google delivery reporter and one delivery-status recipient block binds the exact case customer to `Action: failed` plus permanent `Status: 5.x.x`; an older paused thread still blocks a newer linked thread. Recovery is an explicit authenticated manager action that verifies the case customer, atomically clears every linked pause, and writes an actor-attributed redacted event; service, scheduler, and ingest paths cannot clear it. New/action-needed, aging, and delivery-exception work sends a separate sanitized canonical-link notice to current mapped Machine Managers; an internal operations fallback is used only for unresolved/invalid routing and can never replace the required customer-message CC, while completion stays one customer-facing message with managers CC and no duplicate manager completion. Raw To/CC addresses remain service-only and retention-bound. Hosted-form intake and manual case handling remain independent.
- Deterministic refund follow-up `#687` is implemented for review and remains production-off behind independent Edge and database customer-contact switches. It separates exact missing-field requests from complete-details/no-safe-match copy, uses immutable two-cycle ledgers with at most one request, one reminder, and one receipt acknowledgement per cycle, never asks for wallet/device digits by email, and routes provider setup/outage/rejection/timeout/unknown outcomes only to the current manager action path. Every automatic delivery rechecks the open-case gate and current manager CC route at send time and binds Gmail delivery to the exact source conversation. A stale worker never blindly resends: durable Gmail-sent evidence reconciles the local milestone, while unknown delivery or an abandoned independent reply recheck becomes redacted manager-review work. Only verified direct customer replies can clear waiting state and claim one fact-version recheck; unchanged email facts route to a person instead of being silently applied. Managers see safe reason, requested-field, template-version, delivery-kind, and persisted structured case evidence without raw GPT/provider payloads. GPT prose remains manager-reviewed, and neither the assistant nor scheduler gains payment authority.
- The `#685` manager aging reminder slice is implemented for review and remains production-off behind `REFUND_MANAGER_AGING_NOTICES_ENABLED`. Manager-actionable attention versions allow one current-manager reminder at two Monday-Friday business days and one escalation at five, using the local `America/Los_Angeles` anchor time without holiday inference. Waiting states and verified customer replies invalidate pending work; replies use trusted database creation/message-ID ordering, and only a later manager-relevant re-evaluation restarts aging. Due/current filtering happens before the bounded worker cap so completed rows cannot starve later cases. Every send uses the exact transient route returned by a final database reservation that serializes manager remaps, locks and re-resolves the current active mapping, binds only a non-address mapping fingerprint, and atomically creates the global pre-provider hold. The redacted exact case link and queue selection remain navigation-only; Nayax lookup requires a separate explicit manager request and every official action retains the manager-only fresh-step-up boundary. The hold survives newer versions until its bound known-sent/known-not-sent/unknown settlement; old-version settlement never consumes a newer milestone. Zero-manager milestones create one bounded internal routing exception, uncertain delivery remains durable review with no blind retry, and the assistant gains no customer-contact or payment authority from this lane.
- Gmail local-copy retention `#697` is implemented for review on the current email-assistant stack and remains production-off. A service-only tokenized intent records the exact database-derived quarantine target before any attachment upload; accepted-before-settlement crashes, explicit failures, and unknown outcomes therefore retain deletion coordinates without Gmail. Cleanup uses the database copy time rather than Gmail dates, rechecks the exact private bucket/path before deletion and settlement, and deletes confirmed bytes before redacting metadata. One held object preserves only its own attachment/message while unrelated due storage-free content may purge; aggregate health and every new Gmail copy remain blocked until the hold is resolved. Owner approval of the proposed 180-day policy, the attachment quarantine/scanner version, and integrated synthetic UAT remain required. Disabling or revoking Gmail must not disable retention, and cleanup never changes the source mailbox.
- GPT triage remains default-off and human-review-only. The server runner, strict schema, content-free job ledger, and local developer credential destination are implemented; no production OpenAI secret is configured. A new fail-closed server acknowledgement prevents provider execution until `#635` records the exact OpenAI project retention/data-control mode and privacy/security approval; `store=false` is not treated as zero-retention proof. The database structurally prevents automatic sending.
- Refund operations and customer-refund pilot readiness remain high-sensitivity operational surfaces.
- Access grants that create user-facing Corporate Partner or Technician access must prove both the saved grant and the invite-email attempt; rollout signoff also needs provider/inbox/activation evidence in the linked issue or PR.
- Partner activation uses manual Email Codes and a temporary non-persisting auth session; password creation must succeed before portal sign-in. Hosted signup-confirmation, invite, Email Code, and recovery templates must pass `npm run auth:templates:validate` so email-security prefetch cannot consume credentials. Production Auth delivery uses Bloomjoy's Resend custom SMTP; Supabase's two-email/hour demonstration sender is not an acceptable production fallback.
- Technician management IA uses `/portal/team` for Plus Customer and Corporate Partner self-service, while `/admin/access` remains the Super Admin override surface and the Scoped Admin machine-scoped Technician grant surface. Plus/partner users should not receive `/admin` just to manage Technicians.
- Admin Console IA uses `/admin` as the single internal workspace. The sidebar is the only primary admin navigation map, `/admin` is an exception dashboard rather than a duplicate route launcher, Refunds is one shared core `/refunds` workflow, and Scoped Admins can enter with zero machine grants while machine records/actions remain limited to explicitly granted `reporting_machines`.
- Scoped Admin Technician provisioning is tracked by P0 issues `#536`-`#542`; local executable implementation/UAT belongs in `#537`-`#541`, while `#542` requires post-deploy live invite/account verification.
- Operator Pay, partner reporting, and scheduled exports are active shared foundations.
- Operator report PDF exports depend on the deployed `sales-report-export` Edge Function matching the repo's polished generator; stale deployments can still produce legacy monospaced PDFs and should be redeployed before partner-facing report sharing.
- Timekeeping V1 is a distinct worker-entry and machine-manager-review workflow: monthly completed shifts, per-shift whole-hour rounding, worker-visible correction notes, and issued-statement self-service. Shift review does not execute or alter payments.
- Operator Pay calculation, finalization, payment execution, tax/compliance handling, and provider integration remain separate from the Timekeeping V1 replacement for Sheets/AppSheet.
- Operator pay statements slice `#449` remains the versioned foundation for issued-statement self-service; drafts and manager previews are not worker-visible.
- Authenticated portal bootstrap is shell-first and permission-neutral: Technician entitlement resolution remains authoritative before access reads, access-sensitive navigation waits for hydration, and session-to-shell/dashboard timing contains no account or identity data.
- Frontend work should use existing app patterns plus `PRODUCT.md`, `DESIGN.md`, and `impeccable` when the visible experience matters.

## Safety

- Never paste secrets, raw customer data, payment IDs, vendor exports, or free-text complaint content into docs, issues, PRs, or chat.
